<!doctype html>
<html lang="en">
<head>
    <meta charset="utf-8" />
    <meta http-equiv="X-UA-Compatible" content="IE=edge,chrome=1" />
    <meta content='width=device-width, initial-scale=1.0, maximum-scale=1.0, user-scalable=0' name='viewport' />
    <meta name="viewport" content="width=device-width" />

    <base href="{{ href() }}" />
    <title>{{  text('siteName') }}{{ title ? " - #{title}" : '' }}</title>

    <link rel="apple-touch-icon" sizes="76x76" href="/assets/img/apple-icon.png">
    <link rel="icon" href="/assets/img/favicon.ico">

    <link href="/assets/css/bootstrap.min.css" rel="stylesheet" /> {# Bootstrap core CSS #}
    <link href="/assets/css/animate.min.css" rel="stylesheet"/> {# Animation library for notifications #}
    <link href="/assets/css/paper-dashboard.css" rel="stylesheet"/> {# Paper Dashboard core CSS #}

    {# Fonts and icons #}
    <link href="https://maxcdn.bootstrapcdn.com/font-awesome/latest/css/font-awesome.min.css" rel="stylesheet">
    <link href='https://fonts.googleapis.com/css?family=Muli:400,300' rel='stylesheet' type='text/css'>
    <link href="/assets/css/themify-icons.css" rel="stylesheet">

    {# Pages css #}
    <link rel="stylesheet" href="/assets/css/style.css" />
    {% for item in css %}
        <link rel="stylesheet" href="/assets/css/{{ item }}" />
    {% endfor %}

    {# Core JS Files #}
    <script src="vendor/jquery-2.2.4/dist/jquery.min.js"></script>
    <script src="/assets/js/bootstrap.min.js" type="text/javascript"></script>
    <script src="/assets/js/ckeditor5/ckeditor.js"></script>
    <script src="vendor/sprintf/dist/sprintf.min.js"></script>

    <script src="/assets/js/bootstrap-checkbox-radio.js"></script> {# Checkbox, Radio & Switch Plugins #}
    <script src="/assets/js/bootstrap-notify.js"></script> {# Notifications Plugin #}
    <script src="/assets/js/paper-dashboard.js"></script> {# Paper Dashboard Core javascript and methods #}

    {# Pages JS #}
    {% for item in js %}
        <script src="/assets/js/{{ item }}"></script>
    {% endfor %}
</head>

<body>
{% include 'pages/common/menu.twig' with {'class': 'collapse navbar-collapse off-canvas-sidebar'} %}
<div class="wrapper">
{% include 'pages/common/menu.twig' %}
<div class="main-panel">
    <nav class="navbar navbar-default">
        <div class="container-fluid">
            <div class="navbar-header">
                <div class="row">
                    <button type="button" class="navbar-toggle">
                        <span class="icon-bar bar1"></span>
                        <span class="icon-bar bar2"></span>
                        <span class="icon-bar bar3"></span>
                    </button>
                </div>
                <div class="row">
                    {% if timer is null and userForm is null %}
                        <div class="col-xs-12 text-left navbar-brand-wrapper"><a class="navbar-brand" href="{{ href() }}">{{ text('logoString') }}</a></div>
                    {% else %}
                        <div class="col-md-6">{{ timer|raw }}</div>
                        <div class="col-md-6 text-right">{{ userForm|raw }}</div>
                    {% endif %}
                </div>
            </div>
        </div>
    </nav>


    <div class="content">
        <div class="container-fluid">