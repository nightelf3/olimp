{{ header|raw }}
<div class="card">
    <div class="header">
        <h4 class="title">{{ text('compileErrorTitle') }}</h4>
    </div>
    <div class="content">{{ compileLog }}</div>
</div>
{{ footer|raw }}