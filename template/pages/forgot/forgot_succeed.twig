{{ header|raw }}
<div class="card">
    <div class="header">
        <h4 class="title">{{ text('forgottenSucceedTitle') }}</h4>
    </div>
    <div class="content">
        <p>Привіт, {{ username }}, нові доступи відправлено на {{ email }}</p>
    </div>
</div>
{{ footer|raw }}