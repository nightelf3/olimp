{{ header|raw }}
<div class="card">
    <div class="header">
        <h4 class="title">{{ text('forgottenSucceedTitle') }}</h4>
    </div>
    <div class="content">
        <p>Hello, {{ username }}, new password was sent to your email: {{ email }}</p>
    </div>
</div>
{{ footer|raw }}