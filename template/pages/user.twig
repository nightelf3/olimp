{{ header|raw }}
<div class="card">
    <div class="header">
        <h4 class="title">{{ text('userForm') }}</h4>
    </div>
    <div class="content">
        {{ userForm|raw }}
    </div>
</div>
{{ footer|raw }}