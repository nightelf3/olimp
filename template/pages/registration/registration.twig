{{ header|raw }}
<div class="card">
    <div class="header">
        <h4 class="title">{{ text('registrationForm') }}</h4>
        <p class="category">{{ text('registrationFormHelp') }}</p>
    </div>
    <div class="content">{{ userForm|raw }}</div>
</div>
{{ footer|raw }}